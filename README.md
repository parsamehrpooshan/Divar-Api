# Divar | اپلیکیشن دیوار

## 📝 توضیح کوتاه پروژه

این فایل ها مربوط به بک اند اختصاصی پروژه Divar می‌باشند.
برای دیدن پروژه Divar به آدرس (https://github.com/parsamehrpooshan/divar) بروید.


پروژه دیوار است که با الهام از سایت دیوار توسعه داده شده است. کاربران می‌توانند پس از ورود به حساب کاربری خود، آگهی ثبت کنند و آگهی‌های ثبت شده را در صفحه اصلی و پنل کاربری خود مشاهده کنند.

در این پروژه فرآیند احراز هویت و مدیریت دسترسی کاربران به صفحات مختلف به صورت کامل پیاده‌سازی شده است. همچنین یک پنل مدیریت برای ادمین در نظر گرفته شده که امکان اضافه کردن دسته‌بندی‌های آگهی را فراهم می‌کند.

هدف این پروژه، پیاده‌سازی مفاهیم Authentication، Authorization، مدیریت نقش کاربران (Role-Based Access Control) و کار با API در یک سناریوی واقعی بوده است.

---

## 🖼 دمو پروژه

🎥 دموی آنلاین پروژه (در حال آماده‌سازی...)

📸 پیش‌نمایش پروژه (در حال آماده سازی...)

## 🚀 تکنولوژی‌ها و ابزارهای استفاده‌شده

### ⚙️ تکنولوژی‌ها

* HTML
* CSS
* JavaScript
* React.js
* Next.js (App Router)
* Tailwind CSS
* React Query(Tanstack)
* MongoDB
* Figma
* Axios
* React Hook Form
* Yup

---

## 🛠 روش نصب و اجرای پروژه

### 📥 نصب

1. کلون کردن ریپازیتوری:

```bash
git clone https://github.com/parsamehrpooshan/divar-api
```

2. نصب پکیج ها:

```bash
npm start
```
```bash
npm install --save-dev nodemon
```

3. ران کردن سرور:

```bash
npm start
```

📍 سپس پروژه روی آدرس زیر در دسترس خواهد بود:

```text
http://localhost:3400
```
--
همچنین Swagger روی آدرس http://localhost:3400/swagger در دسترس خواهد بود.

کد تایید پس از وارد کردن شماره موبایل در ترمینال بک اند قابل مشاهده خواهد بود.


## 🧪 ویژگی‌های اصلی پروژه

✨ قابلیت‌ها:

* احراز هویت کاربران (Authentication)
* ورود با شماره موبایل و کد تأیید
* ذخیره‌سازی توکن در Cookie
* پیاده‌سازی JWT Authentication
* مدیریت دسترسی کاربران (Authorization)
* محدودسازی صفحات بر اساس Role کاربران
* نمایش آگهی‌های ثبت شده در صفحه اصلی
* ثبت آگهی جدید از طریق داشبورد کاربری
* دسته‌بندی آگهی‌ها
* پنل اختصاصی مدیریت
* امکان ایجاد دسته‌بندی جدید توسط ادمین
* طراحی ساختارمند و ماژولار کامپوننت‌ها

---

## 👨‍💼 دسترسی ادمین

برای تست بخش مدیریت می‌توانید از شماره زیر استفاده کنید:

```text
09189990099
```

این حساب دارای Role ادمین بوده و به پنل مدیریت دسترسی دارد.
برای رفتن به پنل ادمین به /admin بروید.

امکانات پنل ادمین:

* مشاهده پنل مدیریتی
* افزودن دسته‌بندی جدید برای آگهی‌ها

---


## 📞 اطلاعات تماس

📩 ارتباط با من:

* Email: [parsamehrpooshan@gmail.com](mailto:example@gmail.com)

---

## ✅ نکات پایانی

* این پروژه مستمر آپدیت میشود و بیشتر ارتقا می‌یابد
* خوشحال می‌شوم اگر پیشنهاد یا بازخوردی برای بهبود پروژه داشتید به من اطلاع دهید🙌
* اگر پروژه را دوست داشتید، به ریپازیتوری استار بدید ⭐